import { PagePlaceholder } from "@/components/page-placeholder"

export default function ProfilePage() {
  return (
    <PagePlaceholder
      title="Profile"
      description="Show name, email, groups, and session context from the API-backed profile endpoint."
    />
  )
}
