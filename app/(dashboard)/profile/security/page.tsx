import { PagePlaceholder } from "@/components/page-placeholder"

export default function ProfileSecurityPage() {
  return (
    <PagePlaceholder
      title="Security"
      description="This page will expose the password-change handoff and session details for the current user."
    />
  )
}
