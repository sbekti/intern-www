import { PagePlaceholder } from "@/components/page-placeholder"

export default function DevicesPage() {
  return (
    <PagePlaceholder
      title="Device Management"
      description="Admin users will register MAC addresses and assign each device to the correct VLAN."
    />
  )
}
